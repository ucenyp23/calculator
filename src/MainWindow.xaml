<Window x:Class="calculator.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:calculator"
        mc:Ignorable="d"
        Title="MainWindow" Height="600" Width="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Label Name="Number" Grid.Column="3" Grid.Row="0" FontSize="24" HorizontalContentAlignment="Right" VerticalContentAlignment="Center">0</Label>
        <Button Name="Procent Button" Grid.Column="0" Grid.Row="1" Margin="10" FontSize="24" Click="ClickPercent">%</Button>
        <Button Name="Delete History Button" Grid.Column="1" Grid.Row="1" Margin="10" FontSize="24" Click="ClickPercent">CE</Button>
        <Button Name="Delete Number on the Screen Button" Grid.Column="2" Grid.Row="1" Margin="10" FontSize="24" Click="ClickPercent">C</Button>
        <Button Name="Backspace Button" Grid.Column="3" Grid.Row="1" Margin="10" FontSize="24" Click="ClickPercent">⌫</Button>
    </Grid>
</Window>
